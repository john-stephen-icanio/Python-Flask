name: Python-Flask demo
on:
  push:
    branches: 
      - main
      
jobs:
  Sample-test:
    runs-on: self-hosted
    steps:
      - name: Git Pull
        run: git pull && git checkout main

      - name: test variables
        run: ls

      - name: list variables
        run: echo "${{ vars.COMMANDS }}"

      - name: Run the Commands
        run: eval "${{ vars.COMMANDS }}"
